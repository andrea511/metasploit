@echo off
rem start or stop worker service
rem --------------------------------------------------------
rem check if argument is stop or start

if not ""%1"" == ""START"" goto stop

net start @@BITROCK_WORKER_SERVICE_NAME@@
goto end

:stop

net stop @@BITROCK_WORKER_SERVICE_NAME@@

:end
exit
