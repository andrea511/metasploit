@echo off
rem start or stop thin service
rem --------------------------------------------------------
rem check if argument is stop or start

if not ""%1"" == ""START"" goto stop

net start @@BITROCK_THIN_SERVICE_NAME@@
goto end

:stop

net stop @@BITROCK_THIN_SERVICE_NAME@@

:end
exit
