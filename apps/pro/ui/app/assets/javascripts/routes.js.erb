<%=

JsRoutes.generate({
  include: [
    /vulns/,
    /notifications/,
    /target_count/,
    /brute_force/,
    /attempt_session/,
    /payload_settings/,
    /bruteforce/,
    /task_detail/,
    /^tasks$/,
    /get_session/,
    /^session$/,
    /^host$/,
    /^workspace_credentials$/,
    /^search_workspace_tags$/,
    /^workspace_vuln/,
    /history_workspace/,
    /module_run/,
    /rc_script/,
    /related_modules/,
    /^workspace_module_detail$/,
    /^related_hosts_workspace_vuln$/,
    /^host_tags$/,
    /workspace_notes/,
    /^workspace_services$/,
    /^workspace_vulns$/,
    /^workspace_related_modules$/,
    /^workspace_loots$/,
    /^workspace_brute_force_guess_index$/,
    /^modules$/,
    /^map_host$/,
    /^hosts$/,
    /^pause_task$/,
    /^resume_task$/,
    /^stop_task$/,
    /^stop_paused_task$/,
    /^new_workspace_import$/,
    /^start_scan_and_import$/,
    /^validate_start_scan_and_import$/,
    /^start_import$/,
    /^validate_import$/,
    /^validate_scan_and_import$/,
    /^workspace_nexpose_result_validations$/,
    /^workspace_nexpose_result_exceptions$/,
    /^workspace_nexpose_result_export_runs$/,
    /^push_to_nexpose_status$/,
    /^workspace_nexpose_data_sites$/,
    /^filter_values_workspace_nexpose_data_sites$/,
    /^related_hosts_filter_values_workspace_vuln/,
    /^search_operators_workspace_vuln/,
    /^search_operators_workspace_nexpose_data_sites$/,
    /^workspace_nexpose_data_import_runs$/,
    /^update_last_vuln_attempt_status_workspace_vuln$/,
    /^restore_last_vuln_attempt_status_workspace_vuln$/,
    /^new_webscan$/,
    /^new_scan$/,
    /^new_exploit$/,
    /workspace_services/,
    /workspace_loots/,
    /^new_host$/,
    /^quick_multi_tag_workspace_services$/,
    /^quick_multi_tag$/,
    /^destroy_multiple_hosts$/,
    /^workspace_sonar_imports$/,
    /^filter_values_workspace_sonar_import_fdnss_index$/,
    /^workspace_sonar_import_fdnss_index$/,
    /^workspace_social_engineering_target_list_human_targets$/
  ]
})

%>
